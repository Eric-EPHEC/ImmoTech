@inherits ComponentBase

@*Full-page overlay that shows a progress bar while data is loading *@
@if (IsLoading)
{
    <div class="loading-overlay">
        <progress aria-busy="true"></progress>
    </div>
}

@code {
    [Parameter]
    public bool IsLoading { get; set; }
} 